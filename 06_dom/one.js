const title = document.getElementById();
title.innerHTML ; // gives all the things inside targetted element along with inner html tags
title.textContent; // gives all the text inside the targeted element even though if its not visible, these dont support html tags
title.innerText // only gives visible text, these dont support html tags
document.querySelector(); // just pass the css selectors we usually use for css // always select the first element