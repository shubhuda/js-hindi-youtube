console.log(null>0);
console.log(null>=0);
console.log(null == 0);
/**
 * comparisons(>=, <=, >,<) converts null into number so here null converts it into 0
 * equality works differently than comparison(==)
 */

// strict check
//datatypes are important while comparsion